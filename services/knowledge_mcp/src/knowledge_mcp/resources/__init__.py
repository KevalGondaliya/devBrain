"""Read-only, addressable MCP resources for browsing without invoking a
tool — second-brain-mcp-plan.md §4: `secondbrain://note/{id}`,
`secondbrain://tag/{name}`."""

from __future__ import annotations
