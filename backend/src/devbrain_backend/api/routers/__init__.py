"""One module per `GET`/`POST` surface in DevBrain_vision.md §20/§21. Every
router is thin HTTP plumbing over an existing service/orchestrator/
`devbrain_common` function — see each module's own docstring."""

from __future__ import annotations
